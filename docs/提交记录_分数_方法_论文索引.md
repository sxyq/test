# 提交记录、分数与方法索引（精简版）

> 更新日期：2026-07-09
>
> 精简规则：
> - 每条技术分支只保留最高分或最终代表结果
> - 违规版本单独标注，不作为合规候选
> - 已被同分支更高版本覆盖的低分/异常包不展开

---

## 1. 当前结论

| 项目 | 版本 | score_all | latency | auc | pcoc | 说明 |
|---|---|---:|---:|---:|---:|---|
| **当前合规最佳** | `V207-EMB-INT8-ONLY-sxyq` | **74.03155** | **21.45791s** | **0.75260** | **1.05895** | 只保留 `RepEncoder embedding INT8` 的单变量版，反而打赢了 `V201/V205/V206`；说明当前真正有效的量化方向不是“全量 INT8”，而是更收敛的 embedding INT8 主变量 |
| 合规第二 | `V201-TRT-MOE-UNQUANT-sxyq` | 73.93993 | 21.85059s | 0.75261 | 1.05895 | 外部高分包的原始未量化基线；在 `V207` 出来后，退居为最重要的未量化参考底座 |
| 合规第三 | `V202-TRT-MOE-THROUGHPUT-sxyq` | 71.57366 | 31.99176s | 0.75267 | 1.05932 | 基于 `V201` 的离线批吞吐版：默认开启 `Fused Top2 / Rep count-matmul / Rep LayerNorm-linear fuse / pin_memory + non_blocking H2D / grouped batches`；线上虽然稳定突破 71 分，但明显低于 `V201`，说明这些吞吐默认项并不适合作为当前主线默认配置 |
| 合规第四 | `V194-TRT-MOE-STACKED-sxyq` | 70.95097 | 34.66039s | 0.75261 | 1.05895 | 基于外部 `70.65518` TRT-MoE 包的低冲突重构版，保留 `plugin + count-matmul + token_cap grouping` 主链路，只叠加轻量壳层优化；在 `V201/V207` 出来后，退居为较早一代 TRT 主线样本 |
| 合规第五 | `V195-TRT-MOE-HIGHRISK-sxyq` | 70.48002 | 35.01999s | 0.75271 | 0.86655 | 在 `V194` 基础上继续叠 `force-dataloader + balanced batching + plugin-side FFN prune + bias` 的高风险版；仍高于旧结构主线，但明显低于 `V194`，说明这些变量不能简单继续硬叠 |
| 合规基线 | `V139-FLASH-ATTN-sxyq` | 68.32824 | 45.90067s | 0.847 | 1.05912 | flash-attn安装 |
| 最新线上失败候选 | `V182-FFN25-SVD64FP32-sxyq` | 0 | 39.2778s | 0.75649 | 1.23041 | latency/AUC 都很好，但 **PCOC 超出 1.15 上限**，总分归零 |
| **违规警告** | `V129-SKIP3-BAKED-sxyq` | 69.73428 | 37.93187s | 0.74925 | 1.09351 | 跳3层+bias烘焙，**违规：跳层破坏结构** |

---

## 2. 已线上测试：每条技术分支最高分

### 2.1 合规版本

| 技术分支 | 保留版本 | score_all | latency | auc | pcoc | 结论 |
|---|---|---:|---:|---:|---:|---|
| Dense batched SMoE | `V109-DENSE-SMOE-sxyq` | 67.77 | 48.31s | 0.75247 | 1.05912 | Dense GEMM替代Python循环，1.42x加速 |
| flash-attn安装 | `V139-FLASH-ATTN-sxyq` | 68.33 | 45.90s | 0.847 | 1.05912 | build_env.sh安装flash-attn，-2.4s |
| **SMoE结构化剪枝** | `V153-PRUNE-FFN-sxyq` | **68.53** | **45.02s** | 0.75269 | 1.05995 | dim_ff 1024→768，零开销，AUC微升 |
| SMoE 50%剪枝 | `V154-PRUNE50-sxyq` | 68.45 | 45.25s | 0.75292 | 1.07224 | dim_ff 1024→512，比25%慢0.2s |
| per-user causal SDPA | `V103-PER-USER-CAUSAL-SDPA-sxyq` | 62.99 | 68.79s | 0.75254 | 1.05915 | V109前最佳，被Dense SMoE超越 |
| FP16混合精度 | `V16-FP16EMB-sxyq` | 58.83 | 86.62s | 0.75258 | 1.05914 | FP16权重+embedding，早期主线 |
| embedding_bag | `V24-SPARSE-EMB-sxyq` | 62.06 | 72.75s | 0.75254 | 1.05913 | 合并28 slot为单次embedding_bag |
| silent runner | `V42-SILENT-RUNNER-sxyq` | 62.73 | 69.88s | 0.75254 | 1.05913 | 日志收敛，V103前稳定基线 |
| **重校准+结构叠加** | `V185-V177-STACKED-SUBMIT-sxyq` | **70.16248** | **38.03964s** | **0.75649** | **1.06429** | 在 `V184` 的校准基础上继续吃到结构压缩与运行时叠加收益，AUC/PCOC 保持不变，latency 再降 2.06s，score_all 首次突破 70 |
| **TRT-MoE plugin 主线** | `V207-EMB-INT8-ONLY-sxyq` | **74.03155** | **21.45791s** | **0.75260** | **1.05895** | `V207` 已经在线上打赢 `V201/V205/V206`，说明当前最有效的新变量是 `RepEncoder embedding INT8`，而不是更激进的全量 INT8 或继续整包叠吞吐壳层；后续所有 TRT 主线优化都应先以 `V207` 为当前对照 |

### 2.2 违规版本（仅作警告记录）

| 技术分支 | 保留版本 | score_all | latency | auc | pcoc | 违规原因 |
|---|---|---:|---:|---:|---:|---|
| 层跳过+bias烘焙 | `V129-SKIP3-BAKED-sxyq` | 69.73 | 37.93s | 0.74925 | 1.09351 | **跳层破坏结构** |
| 层跳过+精确修正 | `V128-SKIP3-EXACT-sxyq` | 69.70 | 38.08s | 0.74925 | 1.09351 | **跳层破坏结构** |
| 层跳过2层+精确修正 | `V126-SKIP2-EXACT-sxyq` | 69.59 | 40.49s | 0.75196 | 0.99013 | **跳层破坏结构** |
| 层跳过1层+bias烘焙 | `V125-SKIP1-BAKED-sxyq` | 69.35 | 41.52s | 0.75196 | 0.99013 | **跳层破坏结构** |

### 2.3 失败/无效/异常版本

| 技术分支 | 保留版本 | score_all | latency | 结论 |
|---|---|---:|---:|---|
| batch_size 50→100 | `V140-BATCH100-sxyq` | 68.24 | 46.26s | 无效，+0.36s |
| baddbmm+einsum融合 | `V141-EINSUM-FA-sxyq` | 68.14 | 46.69s | 无效，+0.8s |
| torch.compile SMoE | `V142-COMPILE-SMOE-sxyq` | 15.84 | 270.86s | 灾难：动态shape重编译 |
| QKV permute优化 | `V143-PERMUTE-OPT-sxyq` | 68.42 | 45.50s | 无效 |
| 预计算flash_attn参数 | `V144-PRECOMPUTE-sxyq` | 68.02 | 47.22s | 无效，+1.3s |
| expandable_segments | `V137-SAFE-OPT-sxyq` | 65.95 | 56.11s | 负优化，+8s |
| user_offsets留CPU | `V138-CPU-OFFSET-sxyq` | 66.05 | 55.66s | 负优化，+7s |
| INT8权重量化 | `V147-INT8-QUANT-sxyq` | 67.81 | 48.10s | 负优化，dequant开销>缓存收益 |
| fp16_reduced_precision | `V148-FP16-FAST-sxyq` | 67.86 | 47.90s | 无效，+2s |
| Sparse MoE (V132路径) | `V146-SPARSE-OPT-sxyq` | 63.99 | 64.50s | dispatch开销远超计算节省 |
| CUDA Graph (固定padding) | `V149-CUDA-GRAPH-sxyq` | 66.84 | 52.26s | padding浪费>launch节省 |
| Grouped BMM SMoE | `V150-GROUPED-BMM-sxyq` | 66.84 | 52.26s | GPU→CPU sync 8次/batch |
| No-sync BMM SMoE | `V151-NO-SYNC-BMM-sxyq` | 66.71 | 51.85s | argsort+scatter开销>计算节省 |
| 预分配BMM SMoE | `V152-PREALLOC-BMM-sxyq` | 66.40 | 53.80s | 预分配无效，问题不在内存分配 |
| head_dim剪枝(64→48) | `V156-FIXED-HEAD-PRUNE-sxyq` | 异常 | - | flash_attn不支持head_dim=48 |
| head剪枝(8→6)无修正 | `V157-HEAD-PRUNE-sxyq` | 0 | 44.60s | PCOC=1.475超标，AUC=0.751正常 |
| Gate优化(topk再softmax) | `V136-FUSED-OPT-sxyq` | 0 | 52.94s | PCOC=0.091，gate权重改变 |
| 删除死FFN | `V134-INT8-QUANT-sxyq` | 62.68 | 70.11s | 删除时机错误，权重已加载 |
| INT8量化Dense SMoE | `V165-INT8-DENSE-sxyq` | 56.75 | 95.53s | 反量化开销导致2x变慢 |
| torch.compile全模型 | `V166-CUDA-GRAPH-sxyq` | 68.09 | 46.93s | 无warmup机制，编译开销抵消收益 |
| `V182` 线上实测 | `V182-FFN25-SVD64FP32-sxyq` | 0 | 39.28s | **校准失效**：AUC=0.75649 很好，但 PCOC=1.23041 超上限；结构改动后仍沿用 `log(1/1.05913)` 旧 bias，预测整体偏高 |
| `V186` 异步 prefetch | `V186-ASYNC-PREFETCH-sxyq` | 69.58511 | 40.51412s | **线上未兑现收益**：AUC/PCOC 保持稳定，但相对 `V185` latency 回退 `+2.47s`，说明当前官方环境里 prefetch/H2D overlap 没有转化为正收益 |
| 自适应分层SVD | `V170-ADAPTIVE-SVD-sxyq` | 68.81 | 43.85s | 自适应rank选太小，GEMM效率下降 |
| SVD R=96 | `V172-SVD-R96-sxyq` | 异常 | - | **平台异常**：代码仅V169改R=128→96，V171(R=64)正常69.57分 |
| **head剪枝+V169基线** | `V173-HEAD-PRUNE-sxyq` | 异常 | - | **reshape bug**：attn_out.reshape(B,S,D=512)但head剪枝后只有384元素，崩溃 |
| **head剪枝+FFN剪枝+V169** | `V174-DUAL-PRUNE-sxyq` | 异常 | - | 同V173 reshape bug，head剪枝代码继承了V173的bug |

---

## 3. 已完成最新结果

| 版本 | 分数 | 关键指标 | 方法 | 结论 |
|---|---:|---|---|---|
| `V188-FORCE-DATALOADER-sxyq` | `70.00710` | `PCOC=1.06429 / AUC=0.75649 / latency=38.70555s` | 在 `V187` 基础上默认忽略 `cached_batches`，强制走 `CSV -> Dataset -> DataLoader -> make_collate_fn`，确保 balanced batch 真正进入线上执行路径 | 已线上验证，说明拿回 batch 构造控制权后，调度层优化确实开始生效；但仍略慢于 `V185`，因此继续作为工程叠加底座而非主线替代 |
| `V190-SHAPE-STABLE-sxyq` | `约 69.5` | 精确 PCOC/AUC/latency 待补 | `V188` 的 forced DataLoader 路径 + balanced batching + shape-stable 顺序，并进一步默认开启 `precompute user records`、`fast collate`、`max_sign_id` 单 pass 统计 | 已线上验证但反而降分，不升主线；说明本地 DataLoader 代理收益没有转化为线上端到端收益，CPU batch 构造优化可能被初始化成本、排序策略、真实 CUDA 前向形状或评测计时口径抵消 |
| `V191-PROFILE-TIMING-sxyq` | 本地回退 | profile-only | 基于 `V185` 增加粗粒度分段计时：data prepare、model load、batch prepare、forward sync、collect、batch/warmup 数 | 诊断包，本地分数回退符合预期；价值在于提供端到端耗时证据，不作为晋级主线 |
| `V192-COMPILE-ABLATION-sxyq` | 本地回退 | compile 默认关闭 | 基于 `V185` 做 compile 消融，默认 `GRAB_V130_COMPILE=0`、`GRAB_V179_COMPILE_REP_ENCODER=0`，仍可用环境变量重新打开 | 本地回退，说明“直接全关 compile”并不能简单复现历史消融报告中的收益；需要重新分段计时并核对当前口径差异 |
| `V193-MOE-DISPATCH-sxyq` | 本地回退 | MoE grouped-dispatch 纯 PyTorch | 基于 `V185` 新增 `V193_GROUPED_DISPATCH_MOE=1`，保留 TopK=2 语义，只计算路由命中的 expert token，作为 Fused MoE dispatch/combine + Grouped GEMM 的前置实验 | 本地随机 SMoE 对齐：与 dense all-experts 路径最大误差 `2.98e-08`；但本地分数回退，说明纯 PyTorch grouped-dispatch 还不足以打赢现主线，真正收益仍更可能来自底层 plugin/kernel 实现 |
| `V194-TRT-MOE-STACKED-sxyq` | `70.95097` | `PCOC=1.05895 / AUC=0.75261 / latency=34.66039s` | 基于外部 `70.65518` 包重构为正式提交版，保留其 `TensorRT MoE plugin + RepEncoder count-matmul + token_cap grouping` 主链路，只叠加你们已验证低冲突的 `CPU metadata / chunked collector / adaptive pred_indices / silent runner / optional bias hook` | 已线上验证并直接超过外部底包与 `V185`；当前应把它视为新的主线底座，而不是继续从纯 PyTorch 结构压缩线回退 |
| `V195-TRT-MOE-HIGHRISK-sxyq` | `70.48002` | `PCOC=0.86655 / AUC=0.75271 / latency=35.01999s` | 在 `V194` 基础上继续叠加当前“剩余且可能有效”的贡献：默认忽略 `cached_batches` 强制重建 batch、引入 `balanced user batching`、对 `prepare_trt_moe()` 中的 plugin expert 权重做 `25% FFN prune`、并默认加入 `-0.207347` 输出 bias 重校准 | 已线上验证但低于 `V194`；说明这批变量在 TRT 主链路上不是简单可加，特别是高风险 bias/prune/batch 重构会把 PCOC 压向下边界，后续不应继续把 `V195` 当默认叠加底座 |
| `V196-TRT-MOE-DEBIAS-sxyq` | 待测 | `V195` 去 bias 单变量版 | 保留 `V195` 的 `force-rebuild + balanced batching + plugin-side prune`，只把默认 `BAIDU_LOGIT_BIAS` 从 `-0.207347...` 改回 `0.0` | 用来验证 `V195` 掉分是否主要来自过度负 bias；如果 `PCOC` 明显回升而 latency 基本不变，说明校准是头号问题 |
| `V197-TRT-MOE-NOPRUNE-sxyq` | 待测 | `V195` 去 plugin-prune 单变量版 | 保留 `V195` 的 `force-rebuild + balanced batching + bias`，只关闭默认 `BAIDU_PRUNE_FFN` | 用来验证 `prepare_trt_moe()` 内的 plugin-side `25% FFN prune` 是否是 `V195` latency 回退或校准扰动的主要来源 |
| `V198-TRT-MOE-INWINDOW-BATCH-sxyq` | 待测 | `V194` 规则修正版 | 基于 `V194`，不再叠加 `V195` 的高风险变量；只把 `token_cap / group_factor` 的 batch 聚合移动到 `start inference` 之后的计时区内执行 | 对应你更新后的新规则：允许 batch 聚合，但要求聚合发生在计时区内。这版的意义不是再加速一次，而是先把当前 TRT 主线修回规则一致的提交底座 |
| `V199-TRT-MOE-INWINDOW-FASTMERGE-sxyq` | 本地回退 | `V198` + tensor-cat fast merge 尝试 | 只优化计时区内 `merge_user_batches()` 的实现，尝试减少 Python `tolist()` / list 扩展开销 | 本地 `real_sample_128` 复测失败：`inference time=25.3997s`，比 `V198` 默认更慢，不升主线 |
| `V200-TRT-MOE-INWINDOW-NOGROUP-sxyq` | 本地最佳合规候选 | `V198` 去默认 batch 聚合 | 保持 `V198` 的规则一致性，但默认关闭 `token_cap` 聚合，避免把额外 batch merge 开销硬算进计时区 | 本地 `real_sample_128` 两轮复测都优于 `V198`，且第二轮相对 `V194` 也不回退：`V194=12.2586s`，`V200=9.6989s`；当前本地最值得优先提交验证的合规候选 |
| `V201-TRT-MOE-UNQUANT-sxyq` | `73.93993` | `PCOC=1.05895 / AUC=0.75261 / latency=21.85059s` | 保持外部 73 分包的原始 plugin/kernel 路线，不叠加本地 `bias / balance / prune / force rebuild` 变量，作为新底包基线 | 已线上验证并成为当前总主线：它比 `V202` 再高 `+2.36627` 分、latency 再低 `-10.14117s`，说明当前最强收益来自原生 plugin/kernel 组合本身，后续所有新实验都应先相对它做单变量对照 |
| `V202-TRT-MOE-THROUGHPUT-sxyq` | `71.57366` | `PCOC=1.05932 / AUC=0.75267 / latency=31.99176s` | 基于 `V201` 默认开启 `Fused Top2 / Rep count-matmul / Rep LayerNorm-linear fuse / pin_memory + non_blocking H2D / pinned grouped batches`，并默认使用 `token_cap=300000` 的 batch 聚合 | 已线上验证，但未超过 `V201`：说明“离线批吞吐导向的默认融合与搬运优化”不能直接升为当前主线默认项，应回退为 `V201` 上的可拆单变量候选 |
| `V203-ONLINE-DYNQUANT-sxyq` | `73.84160` | `PCOC=1.05895 / AUC=0.75261 / latency=22.27200s` | 严格基于 `V201` 增加受控 `torch.ao.quantization.quantize_dynamic(...)` 路径，只对 CPU / 非 TRT 场景生效，可选 `all / seq_only / rep_seq_split` 三种量化目标；不继承 `V202` 的吞吐默认项 | 已线上验证，但这不应解读为“量化已在 TRT 主线上生效”：`V203` 只比 `V201` 低 `-0.09833` 分、latency 高 `+0.42141s`，且 PCOC/AUC 完全一致，更符合“线上 CUDA 路径直接跳过 CPU-only dynamic quant，实际仍接近 `V201` 原生主链路”的判断 |
| `V204-SELECTIVE-QUANT-sxyq` | `73.19211` | `PCOC=1.05895 / AUC=0.75261 / latency=25.05551s` | 保留上一编号，但它对应的是“尚未完全收敛到文档边界”的 selective quant 过渡版；成绩已确认低于 `V201` 与 `V203` | 用途是保留版本演进痕迹和反证样本：它说明只要量化语义没有严格切回 `V201` 底座上的 PTQ-prep 路径，就很容易变成分数和 latency 都不占优的中间态，因此后续编号切到 `V205` 继续推进 |
| `V205-SELECTIVE-PTQ-PREP-sxyq` | `73.84305` | `PCOC=1.05895 / AUC=0.75261 / latency=22.26579s` | 严格基于 `V201`，不继承 `V202` 的吞吐默认项；默认推理路径不做 runtime quant，只保留非 MoE dense 子图的 PTQ 候选边界与 manifest 导出能力，明确保持 `MoE plugin` 和 `gate/routing` 为浮点 | 已线上验证：它只比 `V201` 低 `-0.09688` 分、latency 高 `+0.41520s`，且 `AUC/PCOC` 完全一致。这说明文档严格对齐后的 PTQ-prep 边界版没有引入新的明显副作用，也说明真正的量化收益仍然没有在当前 scoring path 中直接兑现 |
| `V206-FULL-INT8-sxyq` | `73.64762` | `PCOC=1.05895 / AUC=0.75261 / latency=23.10335s` | 基于 `V205` 的高风险全量 INT8 研究版：默认叠加 full dynamic int8 研究路径与 `RepEncoder embedding INT8` hook | 已线上验证，但低于 `V201` 与 `V205`：它比 `V201` 低 `-0.29231` 分、latency 高 `+1.25276s`，比 `V205` 低 `-0.19543` 分、latency 高 `+0.83756s`。这说明“激进 INT8 叠加”在当前 TRT/CUDA 主链路上没有兑现端到端收益，应作为高风险反证样本保留 |
| `V207-EMB-INT8-ONLY-sxyq` | `74.03155` | `PCOC=1.05895 / AUC=0.75260 / latency=21.45791s` | 基于 `V205` 去掉全量 INT8，只保留 `RepEncoder embedding INT8` 单变量 | 已线上验证并成为当前总主线：它比 `V201` 高 `+0.09162` 分、latency 再低 `-0.39268s`，比 `V205` 高 `+0.18850` 分、latency 再低 `-0.80788s`。这说明当前真正有效的量化相关变量是 `embedding INT8` 本身，而不是更激进的全量 INT8 叠加 |
| `V208-MAX-STACKED-HIGHRISK-sxyq` | `72.59492` | `PCOC=1.05932 / AUC=0.75267 / latency=27.61493s` | 基于 `V206` 再叠加 `Fused Top2 / rep count-matmul / rep norm-linear fuse / pin_memory + non_blocking / grouped batches / warmup` 的最大叠加版 | 已线上验证，但明显低于 `V207` 与 `V201`：它比 `V207` 低 `-1.43663` 分、latency 高 `+6.15702s`，比 `V201` 低 `-1.34501` 分、latency 高 `+5.76434s`。这说明“尽可能多叠加”的路线会破坏当前主链路收益，应作为最大叠加失败样本保留 |
| `V187-BALANCED-BATCH-sxyq` | `69.85911` | `PCOC=1.06429 / AUC=0.75649 / latency=39.33983s` | 在 `V185` 上继续叠加 balanced batch grouping，固定 `50 user/batch` 不变，仅重排用户分组以拉平每批总 token | 已线上验证，但后续排查确认该版本仍可能命中 `cached_batches` 分支，导致 `Dataset/make_collate_fn` 优化未完全进入真实线上路径，因此不升主线 |

## 4. 待线上测试候选

| 版本 | 技术分支 | 预期 | 风险 |
|---|---|---|---|
| `V189-BUCKET-PREFETCH-sxyq` | 在 `V188` 上继续做端到端链路叠加：shape bucket 式 batch 顺序治理 + CUDA async prefetch | 目标是在已经拿回 DataLoader 路径控制权之后，把 compile warmup 的首批形状、相邻 batch 形状收敛、以及运行时 overlap 一起优化 | 风险在于 `token_total` 近似 bucket 不一定等同于真实最优 kernel shape；同时 prefetch 在官方环境里可能仍是弱收益项，需要线上确认 |
| **`V176-FFN-PRUNE-sxyq`** | V169+FFN剪枝(1024→768)，SVD前执行，**最安全** | ~39-40s, score~70 | V153已验证AUC微升，PCOC几乎不变(1.05995)，bias无需调整 |
| `V175-HEAD-PRUNE-FIX-sxyq` | V173修复reshape bug：reshape(B,S,D)→reshape(B,S,n_heads*head_dim) | ~39-40s | PCOC可能漂移（V169基线下head剪枝PCOC_raw未知），bias=log(1/1.475)可能不准 |
| `V167-AGGRESSIVE-PRUNE-sxyq` | 激进剪枝(FFN40%+head37.5%) | ~38-40s | AUC可能跌破0.65 |
| `V168-SVD-LOWRANK-sxyq` | 纯SVD低秩(R=128,无其他优化) | ~43-45s | 作为SVD独立贡献基线 |

> **突破70分策略**：V169(40.47s)需降至~38.5s。V176=V169+FFN剪枝，V153已证明FFN剪枝零开销且AUC微升，在V169基线上叠加SVD前剪枝可双重受益。V175修复head剪枝reshape bug后也可提交，但PCOC风险较高。

---

## 4. V169 混合方案详解（当前最佳）

### 4.1 优化组合（7项叠加）

| 优化 | 来源 | 作用 |
|---|---|---|
| V109 Dense SMoE | V139基线 | batched GEMM替代稀疏循环 |
| V160 全 FP16 | twelve.zip | model.half()减少内存带宽 |
| V161 禁用 autocast | twelve.zip | 全FP16后autocast冗余 |
| V130 torch.compile | twelve.zip | 只编译seq_encoder+warmup不计时 |
| V141 einsum | twelve.zip | 避免SMoE中间张量 |
| V129 bias 烘焙 | twelve.zip | log(1/1.059)精确修正PCOC |
| V168 SVD低秩 | 新方法 | R=128,Expert权重分解,~50% FLOPs减少 |

### 4.2 关键突破

- V139(45.90s) → V169(40.47s)：降5.43s(11.8%)
- PCOC=1.0018：bias烘焙log(1/1.059)几乎完美
- AUC=0.75239：SVD低秩近似无损精度

### 4.3 V173/V174 异常根因（已修复）

**根因**：head 剪枝后 n_heads=6, head_dim=64，attention 输出维度 = 6×64 = 384，但 forward 中 `attn_out.reshape(B, S, D)` 用的是 D=512（d_model），导致 reshape 崩溃。

**对比**：V158（已验证正常）用的是 `reshape(B, S, self.n_heads * self.head_dim)`（动态计算），而 V173 错误地用了 `reshape(B, S, D)`（固定值）。

**修复**（V175）：`reshape(B, S, D)` → `reshape(B, S, self.n_heads * self.head_dim)`

### 4.4 V176 FFN 剪枝方案

V176 = V169 + V153 FFN 剪枝（dim_ff 1024→768），在 SVD 分解之前执行：
1. FFN 剪枝：按重要性（||fc1||×||fc2||）选取 top-768 神经元
2. SVD 分解：对已剪枝的 [E, D, 768] 权重做 SVD（R=128）
3. 双重受益：FFN 计算量 -25%，SVD 分解的矩阵也更小

V153 已验证：AUC=0.75269（比 V139 的 0.847 更高），PCOC=1.05995（几乎不变），latency 降 0.88s。

---

## 5. 全新方法研究方向（子Agent调研）

### P0 优先级

| 方法 | 核心原理 | 预期加速 | 复杂度 |
|---|---|---|---|
| MetaShuffling+CUDA Graph | token排序→静态GroupedGEMM→CUDA Graph捕获 | 2-3x | 中 |
| Triton持久化GroupedGEMM | 持久化内核+cache-aware排序 | 1.4-2.6x | 中 |
| torch.compile fullgraph+max-autotune | 消除graph break+搜索最优kernel | 1.2-1.5x | 低 |

### P1 优先级

| 方法 | 核心原理 | 预期加速 | 复杂度 |
|---|---|---|---|
| Early Exit(早期退出) | 按样本难度动态决定计算深度 | 1.3-1.8x | 中 |
| Expert缓存+路由预测 | 热门专家常驻+异步prefetch | 1.5-2x | 中 |
| HookMoE分层削专家 | 顶层top-2→top-1+补偿模块 | 1.4x | 中 |

### P2 优先级

| 方法 | 核心原理 | 预期加速 | 复杂度 |
|---|---|---|---|
| DeepFusionKernel | SwiGLU/FFN跨GEMM深度融合 | 1.1-1.3x | 高 |
| MIND自省迭代 | 参数复用迭代+收敛判断 | 1.5-2x | 高 |

---

## 6. 核心教训

1. **零开销减少计算量是唯一有效路径**：V129(跳层)和V153(剪枝)都通过零开销减少计算量获得加速
2. **PyTorch eager mode微优化已达极限**：V140-V148全部失败，permute/flag/量化均无效
3. **Sparse MoE在eager mode不可行**：V146/V150-V152全部失败，sort+scatter+gather开销>4x计算节省
4. **CUDA Graph需要精确shape匹配**：V149固定padding浪费>launch节省；V166 compile无warmup机制
5. **flash_attn不支持非标准head_dim**：V156 head_dim=48导致异常
6. **任何改变计算逻辑的优化都可能破坏PCOC**：V136 Gate优化→PCOC=0.091，V157 head剪枝→PCOC=1.475
7. **bias烘焙是零开销PCOC修正**：V120/V129/V158/V169均使用，log(1/PCOC)加到linear.bias
8. **竞赛规则禁止跳层**：V120-V129虽高分但违规，结构化剪枝是合规替代
9. **混合优化叠加有效**：V169=全FP16+compile+einsum+bias+SVD，7项叠加降5.43s
10. **SVD低秩分解是有效新方法**：V168/V169验证，R=128时GEMM FLOPs减50%且AUC无损
11. **INT8反量化在eager mode有负效果**：V165反量化开销导致2x变慢
12. **torch.compile需要warmup不计时**：V166无warmup机制导致无收益，V169有warmup机制
13. **固定rank优于自适应rank**：V170自适应rank(92%能量)比V169固定R=128慢3.37s
14. **SVD rank非瓶颈**：R=64(40.59s)≈R=128(40.47s)，attention计算才是真正瓶颈
15. **head剪枝forward必须用动态reshape**：V173用reshape(B,S,D=512)崩溃，V158用reshape(B,S,n_heads*head_dim)正常
16. **FFN剪枝与SVD可叠加**：V176=V169+FFN剪枝，剪枝在SVD前执行，双重受益且不改变PCOC
17. **⚠️ 打包违规是"平台异常"的头号嫌疑犯**：V172/V173/V174/V176/V177/V178全部因zip含外层文件夹导致平台找不到infer.py。**每次打包后必须用 `unzip -l` 验证文件在根目录**。打包必须从版本目录内部执行：`cd Vxxx && zip -r ../Vxxx.zip requirements.txt build_env.sh infer.py`
18. **⚠️ V176/V177/V178"FFN剪枝+SVD组合异常"是误判**：原推断FFN剪枝+SVD组合有问题（基于V177 bias=0和V178 SVD FP32都异常），实际三者异常都是打包违规。V177/V178的排查实验基于错误前提，结论无效。V176代码本身可能完全正常，需重新提交验证
19. **⚠️ `V182` 证明“本地 proxy 更快”不等于线上可用**：`prune25 + rank64 + FP32-SVD` 在线上拿到 39.28s / AUC 0.75649，但 PCOC=1.23041 直接归零；问题不在排序能力，而在输出校准漂移
20. **结构改动后不能继续复用旧 bias**：`V182` 仍使用 `GRAB_V129_LOGIT_BIAS=log(1/1.05913)`，但线上实际需要的校准更接近 `log(1/1.23041)`；等价于还缺约 `-0.1499` 的额外负 bias
21. **`V184` 证明 `V182` 的主要漂移是常数级校准偏移**：将默认 bias 从 `-0.05745` 调整到 `-0.20735` 后，PCOC 从 `1.23041` 降到 `1.06429`，AUC 仍保持 `0.75649`
22. **`V184` 把校准问题修回了合规区间**：相对 `V169`，`V184` 把 latency 从 `40.47221s` 降到 `40.09613s`，同时 AUC 从 `0.75239` 提到 `0.75649`
23. **`V185` 证明 `V177` 这条诊断分支可以被重构成正式主线**：在保留 `V184` 相同 `AUC=0.75649`、`PCOC=1.06429` 的前提下，latency 进一步降到 `38.03964s`，score_all 提升到 `70.16248`
24. **当前最佳合规路线已经从“先修校准”进入“校准稳定后继续吃运行时收益”阶段**：接下来主线不该回退到无 bias 诊断，而应沿着 `rank64 + FP32-SVD + calibrated bias + compile/runtime stack` 继续推进
25. **`V186` 说明“文档里允许的 I/O 优化”不等于线上一定有效**：异步 prefetch 在实现上合规，但官方环境中没有带来正向收益，因此这条线目前只能保留为已验证样本，不能升为主线
